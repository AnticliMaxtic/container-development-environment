nodeLinker: node-modules

# Yarn Berry configuration for this tooling-only repository.
# Using node-modules linker (instead of PnP) because this is primarily
# a Docker/infrastructure project, not a JavaScript application.
